# ZDR 代理指南

本仓库维护各家 LLM provider 的数据处理政策台账。每条结论都必须能追到官方原文的那一句。

## 校验

```bash
bun run validate
```

每次修改后都要跑。PR 合并前必须通过。

## 两个概念:抓取产物 vs 结论

| | 抓取产物 | 结论 |
| --- | --- | --- |
| **是什么** | 机器抓回来的政策原文和抓取记录 | 我们对厂商做了什么承诺的判断 |
| **位置** | `documents/` `health/` | `providers/<id>.toml` |
| **谁写** | 只有抓取流程写 | agent 或人 |
| **能不能手改** | **绝对不能** | 能,但要有证据 |

`documents/` 里的文件是内容寻址的不可变快照,手改一个字就断了整条可追溯性链。
要改来源、结论或锚点,都改对应的 `providers/<id>.toml`;新来源先用正式抓取器建立
baseline,再让锚点引用它。

## 五个维度

| 维度 | 问什么 |
| --- | --- |
| `training` | 会不会拿 API 输入去训练模型 |
| `retention` | 请求内容留多久 |
| `zdr` | 能不能做到零数据保留 |
| `processing_region` | 数据在哪里处理 |
| `role` | 协议上是受托处理方还是独立控制方 |

## 目录结构

```
providers/<provider_id>.toml                被监控的官方 URL、产品档位、五维结论和锚点
documents/<source_id>/<version_id>.md       政策正文,不可变
documents/<source_id>/history.toml          版本记录 + 当前版本指针,本目录唯一可变的文件
health/<source_id>.toml                     当前抓取异常,恢复后删除
```

`source_id` = `sha256(url)` 前 16 位。`[[source]]` 只写 URL 和策展元数据，**不要再写
`id` 字段**；加载时会由 URL 派生。

## 锚点(阻断项)

**每条结论必须绑定至少一个锚点。** 追不到原文的结论等于没有。

锚点用 W3C Web Annotation 的 TextQuoteSelector:

```toml
source_id = "9fa678c2f42aae99"
topics = ["zdr", "retention"]

[selector]
prefix = "1.4 如您使用本服务企业版，包括 Coding Plan 企业版与Agent Plan 企业版，"
exact = "您的数据（您与本服务交互的数据）将不会被本服务留存"
suffix = "，请您自行存储和备份。本服务将不会收集和使用您的数据进行服务优化。"
```

- `exact` 是支撑结论的那一句,必须逐字取自正文
- `prefix` / `suffix` 前后各约 60 字。**不足就用实际长度,空也合法**
  (条款在文档开头或结尾时本来就没有上下文)
- 匹配在 NFKC 折叠、剥掉所有非字母数字之后做,所以 markdown 的 `#` `*` `[]` 不影响命中
- 如果 `exact` 在正文里出现多次,必须靠 prefix/suffix 唯一定位

三态:

| 状态 | 含义 | 动作 |
| --- | --- | --- |
| `exact` | 原句还在,位置可以变 | 无事发生 |
| `reworded` | prefix/suffix 还在,中间那句改了 | 这是政策变化 |
| `gone` | prefix/suffix 也找不到 | 开 issue,**不自动改结论** |

`gone` 时**不得改绑到最新版本、不得伪造 prefix/suffix**。
条款找不到 ≠ 厂商撤销了承诺。

**判 `gone` 之前,必须先确认这次抓取的 classify 结果是 `fetched`。**
抓取失败或拿到错误壳时,锚点状态保持上一次的值。实测过:不做这道检查,
54 个锚点第一遍会报 4 条假 `gone`,重跑就全中——那 4 条是 r.jina.ai 在并发下
返回的 HTTP 200 错误壳。

## 权威性:逐维度声明

同一家的不同文档对同一维度可能说不同的话。结论与其证据锚点都写在 provider 文件里:

```toml
# providers/volcengine.toml 中对应 product 的维度
[product.zdr]
anchor = "zdr-enterprise"
```

全局默认优先级:

```
contract > developer_data > enterprise_privacy_faq > consumer_privacy > official_blog
```

两条硬规则:

- **`third_party` 永远不能当主证据**,最多算发现线索
- **web.archive.org 快照不能当权威来源**,要找现行官方 URL

## 三种「没有」必须分清

最容易出错的地方:

| 情况 | 数据层怎么记 | 页面 |
| --- | --- | --- |
| **未披露** — 查了 A、B、C 三处,厂商没表态 | verdict 记下查阅范围 `searched[]` | ✗(保守假设) |
| **明确否定** — 厂商说了「会用于训练」 | 正常 verdict,值为否 | ✗(有依据) |
| **抓取失败** — 我们没拿到文档 | **只更新 `health/`,不产生结论** | 保留最后确认状态,开 issue |

**抓取失败是关于我们的事实,不是关于厂商的结论。**
绝不允许因为抓不到就记成「未披露」——那是把我们的能力问题伪装成厂商的披露问题。

图片或 PDF 中的条款可以由 agent 用 OCR 辅助调查,但 OCR 结果本身不是官方原文快照。
正式锚点应优先绑定可稳定复核的官方文本；只能使用图片时,必须保留原始官方文件和页码,
由 agent 明确标注证据形态,不能把每次 OCR 输出当成新的厂商版本。

## 档位要分开

同一家的 API 和订阅套餐,政策常常完全相反。火山是最典型的:

| 档位 | 条款 | 结论 |
| --- | --- | --- |
| 按量 API | 方舟服务条款 | 不用于训练,无零保留承诺 |
| Coding Plan **企业版** | 订阅套餐专用条款 §1.4 | 数据「将不会被本服务留存」 |
| Coding Plan **个人版** | 订阅套餐专用条款 §1.3 | 反过来授权训练,且不可撤销 |

同一份文档相邻两节,结论相反。混成一行就全错了。
不同档位在同一 provider 文件中建独立 `[[product]]`,并在证据说明中写清适用范围。

国内站和国际站同理:不同法律实体、不同数据处理地,分开建条目。

## 保留期限要写清对象

「日志至少留存六个月」和「输入输出六个月后删除」是两件事。`retention` 必须写明:

- **对象**:输入输出正文 / KV 缓存 / 访问日志
- **语境**:默认 / 启用某功能后 / 例外
- **期限类型**:下限 / 上限 / 典型区间

国内站普遍有「相关网络日志不少于六个月」(《网络安全法》),那是**下限**,
不能反推出「六个月后删除全部内容」。

## 多跳链路

有些 provider 是转售或聚合。

- 按实际产品配置组合各跳,**任一跳明确为否,则全链为否**
- 不从「网关」「托管」这类分类去猜谁能访问内容
- 每跳各自可申请 ZDR,**不自动证明能在同一配置下同时开通**

## 审查清单(阻断项)

| 阻断项 | 为什么 |
| --- | --- |
| 结论没有绑定锚点 | 追不到原文的结论等于没有 |
| 锚点的 `exact` 在当前正文里定位不到 | 锚点是坏的 |
| 用 `third_party` 或 archive.org 当主证据 | 只能当发现线索 |
| 因为抓不到就写成「未披露」 | 伪装能力问题为披露问题 |
| 锚点 `gone` 却自动改了结论 | 条款找不到 ≠ 撤销承诺 |
| 同一家不同档位混成一行 | 火山企业版和个人版是相反的 |
| 修改了 `documents/` | 不可变的抓取产物 |
| `[[source]]` 里写了 `id` 字段 | source id 应由 URL 派生 |

## 不要做的

- 不要猜。信息不足就开 issue 说缺什么,不要填一个看起来合理的值
- 不要为了填满五个维度而降低证据标准
- 不要跑 `git commit` / `git push` / `gh pr create`,workflow 负责这些
- 不要扩大 issue 的范围,除非额外改动是内部一致性必需的,且每条都独立验证过

## 新建 provider 时的层级

公司 → 产品线(一个 `providers/*.toml`)→ 档次(一个 `[[product]]`)。

**不同层级适用的是不同的协议**,所以编码订阅这类产品线要独立成文件,不要塞进母
公司的文件——它们有自己的订阅专用条款和隐私说明,混在一起引文会跨产品串味。
用 `company` / `company_name` 归并,详情页会自动聚成一页。

详见 docs/scope.md「三层结构」。
